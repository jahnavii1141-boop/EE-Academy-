// SEOHead is a no-op in Next.js — metadata is handled via the Metadata API in each page file.
// This stub keeps existing page imports from breaking during migration.
export default function SEOHead() {
  return null
}
